fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
httpx>=0.27.0
python-multipart          # OAuth2PasswordRequestForm
passlib[bcrypt]           # password hashing
python-jose[cryptography] # JWT tokens
# Optional — only needed for real gateway integration:
# razorpay
# portone-python
